[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x", "openssl"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npx prisma generate && npx prisma db push --accept-data-loss"]

[start]
cmd = "cd backend && node src/index.js"
